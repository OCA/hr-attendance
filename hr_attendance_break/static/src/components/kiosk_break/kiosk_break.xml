<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="hr_attendance_break.KioskBreak">
        <t t-if="this.props.employeeData">
            <div
                class="o_hr_kiosk_mode_main d-flex flex-grow-1 justify-content-center align-items-center"
            >
                <div class="card rounded-3">
                    <div class="card-body rounded-3 text-center">
                        <t t-call="hr_attendance.EmployeeBadge">
                            <t
                                t-set="employeeAvatar"
                                t-value="this.props.employeeData.employee_avatar"
                            />
                            <t t-set="employeeAvatarHeight" t-value="'60'" />
                        </t>
                        <h3 class="mt-2 mb-1">
                            <t t-esc="this.props.employeeData.employee_name" />
                        </h3>
                        <h5 class="text-muted my-0">
                            <t
                                t-if="this.props.employeeData.on_break"
                            >You are currently on a break</t>
                            <t t-else="">What would you like to do?</t>
                        </h5>
                        <div class="mt-4 d-flex flex-column gap-2">
                            <button
                                t-on-click="() => this.props.onToggleBreak()"
                                t-attf-class="o_hr_attendance_break_kiosk_button btn btn-lg {{ this.props.employeeData.on_break ? 'btn-success' : 'btn-secondary' }}"
                            >
                                <i
                                    class="fa me-1"
                                    t-att-class="this.props.employeeData.on_break ? 'fa-play' : 'fa-pause'"
                                />
                                <t t-if="this.props.employeeData.on_break">End Break</t>
                                <t t-else="">Start Break</t>
                            </button>
                            <button
                                t-on-click="() => this.props.onCheckout()"
                                class="btn btn-warning btn-lg"
                            >
                                <i class="fa fa-sign-out me-1" />
                                <span>Check out</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="o_hr_kiosk_mode_bottom my-4 mx-auto">
                <button
                    class="btn btn-light btn-lg rounded-pill px-5"
                    t-on-click="() => this.props.onClickBack()"
                >
                    <i class="oi oi-chevron-left me-2" />
                    <span>Go back</span>
                </button>
            </div>
        </t>
    </t>
</templates>
