This module adds support for `hr_employee_calendar_planning` and `flexible_hours` so that overtime for attendance is calculated correctly.

You only need to use this module if you use flexible hours.
