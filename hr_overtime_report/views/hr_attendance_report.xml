<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2025 Moduon Team S.L.
     License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0) -->
<odoo>
    <!-- Pivot view-->
    <record id="hr_attendance_overtime_view_pivot" model="ir.ui.view">
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <pivot disable_linking="True">
                <field name="employee_id" type="row" />
                <field name="date" interval="month" />
                <field name="duration" type="measure" />
            </pivot>
        </field>
    </record>

    <!-- List view-->
    <record id="hr_attendance_overtime_view_list" model="ir.ui.view">
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <list>
                <field name="employee_id" />
                <field name="company_id" optional="hidden" />
                <field name="date" />
                <field name="duration" widget="float_time" />
            </list>
        </field>
    </record>

    <!-- Calendar view-->
    <record id="hr_attendance_overtime_calendar_view" model="ir.ui.view">
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <calendar string="Overtime Calendar" date_start="date" color="employee_id">
                <field name="employee_id" />
                <field name="duration" />
            </calendar>
        </field>
    </record>

    <!-- Graph view-->
    <record id="hr_attendance_overtime_graph_view" model="ir.ui.view">
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <graph string="Overtime Graph">
                <field name="date" type="row" />
                <field name="duration" type="measure" />
                <field name="employee_id" type="col" />
            </graph>
        </field>
    </record>

    <!-- Search view-->
    <record id="hr_attendance_overtime_view_search" model="ir.ui.view">
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <search>
                <field name="employee_id" />
                <field name="date" />
                <filter
                    name="filter_date_from"
                    date="date"
                    default_period="month"
                    string="Date"
                />
                <separator />
                <filter
                    string="Active Employees"
                    name="active_employees"
                    domain="[('employee_id.active', '=', True)]"
                />
                <filter
                    string="Positive Overtime"
                    name="positive"
                    domain="[('duration', '&gt;', 0)]"
                />
                <filter
                    string="Negative Overtime"
                    name="negative"
                    domain="[('duration', '&lt;', 0)]"
                />
                <group expand="0" string="Group By">
                    <filter
                        string="Employee"
                        name="group_employee"
                        context="{'group_by': 'employee_id'}"
                    />
                    <filter
                        string="Date"
                        name="group_month"
                        context="{'group_by': 'date:month'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <!-- Action-->
    <record id="action_hr_attendance_overtime" model="ir.actions.act_window">
        <field name="name">Overtime Report</field>
        <field name="res_model">hr.attendance.overtime</field>
        <field name="view_mode">pivot,list,calendar,graph</field>
        <field name="search_view_id" ref="hr_attendance_overtime_view_search" />
        <field
            name="context"
        >{'search_default_active_employees': 1, 'create': False}</field>
    </record>

    <!-- Menuitem-->
    <menuitem
        id="hr_attendance_overtime_menu"
        name="Extra Hours"
        parent="hr.hr_menu_hr_reports"
        action="action_hr_attendance_overtime"
        sequence="30"
    />
</odoo>
